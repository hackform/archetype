version: '3'

services:
  whoami:
    image: containous/whoami
    networks:
      - mithlond-net
    deploy:
      mode: replicated
      replicas: 1
      labels:
        - "traefik.enable=true"
        - "traefik.docker.network=mithlond-net"
        - "traefik.port=80"

networks:
  mithlond-net:
    external: true
